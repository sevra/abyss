include $(GNUSTEP_MAKEFILES)/common.make

TOOL_NAME = gravity

ADDITIONAL_LDFLAGS = -lcurses -llua

gravity_OBJC_FILES = $(wildcard *.m)

-include GNUmakefile.preamble

include $(GNUSTEP_MAKEFILES)/tool.make

-include GNUmakefile.postamble
